# consent-protocol/.env.example
# Copy to .env and fill in. Keys here MUST match exactly what the code reads.
# See docs/reference/env_and_secrets.md and hushh_mcp/config.py.

# =============================================================================
# REQUIRED BY RUNTIME (hushh_mcp/config.py, server.py, db/, api/)
# =============================================================================

# HMAC signing key — must be at least 32 characters (64-char hex recommended)
SECRET_KEY=your_64_char_hex_here

# Vault AES key — exactly 64-character hex (256-bit)
VAULT_ENCRYPTION_KEY=your_64_char_hex_here

# Database (Supabase Session Pooler) — used by db/connection.py, db/db_client.py
DB_USER=postgres.your-project-ref
DB_PASSWORD=your-password
DB_HOST=aws-1-us-east-1.pooler.supabase.com
DB_PORT=5432
DB_NAME=postgres

# CORS / server — used by server.py
FRONTEND_URL=http://localhost:3000

# Firebase Admin SDK — used by api/utils/firebase_admin.py (auth)
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}

# Google Generative AI — used by config.py and Gemini/Vertex services
# GOOGLE_API_KEY=your-google-api-key
GOOGLE_GENAI_USE_VERTEXAI=True

# =============================================================================
# OPTIONAL (defaults in code)
# =============================================================================

# Expiration (milliseconds) — hushh_mcp/config.py
DEFAULT_CONSENT_TOKEN_EXPIRY_MS=604800000
DEFAULT_TRUST_LINK_EXPIRY_MS=2592000000

# Consent timeout (seconds) — api/routes/sse.py, developer.py
CONSENT_TIMEOUT_SECONDS=120

# App context — hushh_mcp/config.py
ENVIRONMENT=development
AGENT_ID=agent_hushh_local
HUSHH_HACKATHON=enabled

# Server / Swagger — server.py
# PORT=8000
# ROOT_PATH=

# =============================================================================
# PARITY: Migrations and scripts use the same DB_* above (no DATABASE_URL).
# =============================================================================
# db/migrate.py uses get_database_url() from db.connection.
