{
  "_comment": "Repo-local Python fallback example. Public install should prefer 'npx -y @hushh/mcp' once the npm package is published. Run 'python setup_mcp.py' to generate local Python config automatically.",
  "_instructions": "Or replace <PATH_TO_CONSENT_PROTOCOL> with your actual directory path",
  "mcpServers": {
    "hushh-consent": {
      "command": "python",
      "args": ["<PATH_TO_CONSENT_PROTOCOL>/mcp_server.py"],
      "env": {
        "PYTHONPATH": "<PATH_TO_CONSENT_PROTOCOL>"
      }
    }
  }
}
