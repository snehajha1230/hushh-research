{
  "_comment": "Run 'python setup_mcp.py' to generate this config automatically with correct paths",
  "_instructions": "Or replace <PATH_TO_CONSENT_PROTOCOL> with your actual directory path",
  "mcpServers": {
    "hushh-consent": {
      "command": "python",
      "args": ["<PATH_TO_CONSENT_PROTOCOL>/mcp_server.py"],
      "env": {
        "PYTHONPATH": "<PATH_TO_CONSENT_PROTOCOL>"
      }
    }
  }
}
